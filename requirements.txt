# this too could be contained in setup.cfg
# and added as dev requirements
# and installed only when needed
# but installing it with pip separately
# gives the flexibility to add editable
# dependencies to the repo

pytest  # python testing
pytest-cov  # test coverage check

sphinx  # documentation generator
snooty-lextudio  # rst server
rstcheck  # rst linter

autopep8  # autoformat
pylint  # python linter
pylama  # python linter group

build  # create packages
twine  # check packages

-e .
-r ./git_subm_demo/requirements.txt
